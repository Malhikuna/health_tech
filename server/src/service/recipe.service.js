import {validate} from "../validation/validation.js";
import {getRecipeValidation} from "../validation/recipe.validation.js";
import {recipeRouter} from "../route/recipe.routes.js";

const getRecipe = async (id) => {
}

export default {
  getRecipe
};